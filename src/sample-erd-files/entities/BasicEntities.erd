User
    username text(50)
    password text(128)
    email text(100)
    active bool

Task
    description text(200)
    done bool

EntityWithoutProperties

EntityWithCustomIdProperty
    # You can customize identifier's name by defining it explicitly.
    # However, only one property with "identifier" type is allowed.
    customIdProperty identifier
