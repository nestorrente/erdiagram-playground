Company
    name text(30)
    foundationDate date
    logoImage blob
    active bool

Employee
    name text(50)
    birthdate? date
    salary decimal(10, 2)
    active bool

Company <->* Employee

Employee *<->? Employee boss
