Company
    name text(30)
    foundationDate date
    active bool

Employee
    name text(50)
    birthdate? date
    salary decimal(10, 2)
    active bool

Company <->* Employee

Employee *<->? Employee boss
